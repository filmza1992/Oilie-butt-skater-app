<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Specify HTTP traffic to be allowed -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust preinstalled CAs -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>